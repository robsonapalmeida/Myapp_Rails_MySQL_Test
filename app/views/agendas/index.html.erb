<p id="notice"><%= notice %></p>

<h1>Agendas</h1>

<table>
  <thead>
    <tr>
      <th>Nome Empleado</th>
      <th>Email Empleado</th>
      <th>Observacion</th>
      <th>Agenda</th>
      <th>Tipo Servico</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @agendas.each do |agenda| %>
      <tr>
        <td><%= agenda.nomeempleado %></td>
        <td><%= agenda.emailempleado %></td>
        <td><%= agenda.observacion %></td>
        <td><%= agenda.agenda %></td>
        <td><%= agenda.tiposervico %></td>
        <td><%= link_to 'Show', agenda %></td>
        <td><%= link_to 'Edit', edit_agenda_path(agenda) %></td>
        <td><%= link_to 'Destroy', agenda, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Agenda', new_agenda_path %>
